# This component needs scipy.stats assuming MYSQL-python, phpserialize, sqlparse and numpy are installed already
#python version is 2.7
#follwings are command to run on Unix to install required libraries
sudo /usr/bin/pip2 install -U scipy=0.18.1

